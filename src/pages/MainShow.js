//show page for either movies or the show. should we separate movies and shows?
//may be easier to show different content, we can rename to MovieShow and add SeriesShow 
//or we can use conditional logic to show elements for series vs show

const MainShow = () => {
  return (
    <div>Main Show Page</div>
  )
}

export default MainShow